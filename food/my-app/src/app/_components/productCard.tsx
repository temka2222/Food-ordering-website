import { useFoods } from "./foodsProvider"

 type FoodsType = {
 foodName:string,
    price:number,
    image:string,
    ingredients:string,
    category:string
};
   export const FoodCard=({ foodName,price,image,ingredients,category }: FoodsType)=>{
    
        return(


        )

    }